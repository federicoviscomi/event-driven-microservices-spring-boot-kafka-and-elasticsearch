spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/fevi/microservices-demo_origin/config-server-repository
          clone-on-start: true
      fail-fast: true